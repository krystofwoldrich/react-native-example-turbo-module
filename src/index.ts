//@flow
import { NativeModules } from 'react-native'

export default NativeModules.Calculator;

//
// At this point the library is ready to be used.
// Just create a new project and import it.
// (This probably will stop working down the line
// when the default modules will be Turbo Module):
//
